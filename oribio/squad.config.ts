import { defineConfig } from '@bradygaster/squad-sdk';

export default defineConfig({
  team: {
    name: 'oribio-squad',
    root: '.squad',
    description: 'Equipo multi-agente para Oribio — marca colombiana de renovación capilar. Coordina desarrollo de SuperApp, landing pages, MCP server, image creation y operaciones.',
  },
});

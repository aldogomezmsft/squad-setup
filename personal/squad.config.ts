import { defineConfig } from '@bradygaster/squad-sdk';

export default defineConfig({
  team: {
    name: 'aldo-personal-squad',
    root: '.squad',
    description: 'Personal squad for Microsoft projects and side work. Focus on developer tooling, internal tools, and productivity.',
  },
});
